version: 2
jobs:
    test:
        docker:
            - image: circleci/rust:latest
        steps:
            - checkout
            - run: cargo test

workflows:
    version: 2
    test:
        jobs:
            - test