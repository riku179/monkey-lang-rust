version: 2.1

orbs:
    rust: glotrade/rust@0.1.1

jobs: 
    test:
        steps:
            - checkout
            - rust/test:
                  toolchain: "nightly"
                  release: false
